name: Deploy Backend

on:
  push:
    branches: [ deploy ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            echo "🚀 Starting backend deployment..."
            
            # 백엔드 디렉토리로 이동
            cd /home2/handongjudge/docker/Handongjudge_BE
            
            # 최신 코드 가져오기
            echo "📥 Pulling latest code..."
            git pull origin deploy
            
            # Docker 컨테이너 재빌드 및 재시작
            echo "🔨 Building and restarting Docker container..."
            cd /home2/handongjudge/docker
            sudo docker compose build --no-cache cl_backend
            sudo docker compose up -d cl_backend
            
            echo "✅ Backend deployment completed!"

